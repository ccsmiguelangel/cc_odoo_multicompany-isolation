<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="res_partner_isolation_rule" model="ir.rule">
            <field name="name">Strict Partner Multi-Company Isolation</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="product_template_isolation_rule" model="ir.rule">
            <field name="name">Strict Product Template Multi-Company Isolation</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="product_product_isolation_rule" model="ir.rule">
            <field name="name">Strict Product Multi-Company Isolation</field>
            <field name="model_id" ref="product.model_product_product"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>
    </data>
</odoo>
